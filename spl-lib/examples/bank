project { "x": $in.deposit, "y": $in };
split (pure "bank");
project { "x": $in.y.transfer1, "y": $in.y };
split (pure "bank");
if (project $in.x == false) {
  project "First transfer failed."
} else {
  project $in.y.transfer2;
  pure "bank"
}
